<ui:FluentWindow x:Class="DeskCycle.Desktop.Views.SessionWindow"
                xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                xmlns:sp="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
                Title="Einheit"
                Height="600" Width="860"
                WindowBackdropType="Mica"
                WindowCornerPreference="Round"
                ExtendsContentIntoTitleBar="True"
                WindowStartupLocation="CenterOwner"
                TextOptions.TextFormattingMode="Display"
                Loaded="OnLoaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Einheit" ShowMaximize="False" ShowMinimize="False" />

        <Grid Grid.Row="1" Margin="24,8,24,24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding Title}" FontSize="20"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Grid.Row="1" Text="{Binding Summary}" Margin="0,6,0,16" FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <Border Grid.Row="2" CornerRadius="8" ClipToBounds="True"
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1">
                <sp:WpfPlot x:Name="CadencePlot" />
            </Border>

            <Grid Grid.Row="3" Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Widerstandsstufe" VerticalAlignment="Center" FontSize="12"
                           Margin="0,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:TextBox Grid.Column="1" Width="60" VerticalAlignment="Center"
                            Text="{Binding ResistanceLevel, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Column="2" Text="Notiz" VerticalAlignment="Center" FontSize="12"
                           Margin="20,0,10,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:TextBox Grid.Column="3" VerticalAlignment="Center"
                            PlaceholderText="Optional"
                            Text="{Binding Note, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Column="4" Text="{Binding Status}" VerticalAlignment="Center" FontSize="12"
                           Margin="14,0" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:Button Grid.Column="5" Content="Speichern" Appearance="Primary" Click="OnSaveClick" />
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
